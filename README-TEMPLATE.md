# New-project bootstrap (copy into any new repo root — CLAUDE.md/GEMINI.md are SYMLINKS to AGENTS.md (cp -a to preserve them))
1. Fill in CLAUDE.md invariants (keep under a page).
2. mkdir docs && ./scripts/generate-state.sh   (then wire it into .git/hooks/post-commit)
3. Done: every AI tool auto-loads its pointer file; any other AI gets ONBOARD.md pasted once.
